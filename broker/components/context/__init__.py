"""Context building sub-package.

Modules:
    builder  — ContextBuilder, SafeFormatter
    providers — ContextProvider and built-in provider implementations
    tiered   — TieredContextBuilder, BaseAgentContextBuilder
"""
