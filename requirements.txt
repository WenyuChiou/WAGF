# Dependencies are managed in pyproject.toml (single source of truth).
# Install: pip install -e .            (core only)
# Install: pip install -e ".[llm]"     (+ LLM providers)
# Install: pip install -e ".[analysis]" (+ matplotlib, scipy, etc.)
# Install: pip install -e ".[full]"    (everything)
#
# This file is kept for tools that only understand requirements.txt.
# Regenerate: pip-compile pyproject.toml -o requirements.txt

pydantic>=2.0
pyyaml>=6.0
numpy>=1.24
pandas>=2.0
requests>=2.28
