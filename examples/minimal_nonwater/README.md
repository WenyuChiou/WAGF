# Minimal Non-Water Reference Example

This example demonstrates that WAGF can be configured with generic, non-water constructs.

- Framework: `generic`
- Constructs: `RISK_LABEL`, `CAPACITY_LABEL`
- Actions: `adapt_strategy`, `hold_position`

Run:

```bash
python examples/minimal_nonwater/run_demo.py
```
